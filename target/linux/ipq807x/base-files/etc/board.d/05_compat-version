
. /lib/functions.sh
. /lib/functions/uci-defaults.sh

board_config_update

case "$(board_name)" in
	xiaomi,ax3600|\
	redmi,ax6)
		ucidef_set_compat_version "2.0"
		;;
esac

board_config_flush

exit 0
